spring:
  application:
    name: ms7
  cloud:
    config:
      enabled: true
      uri: http://config-server:8888  # Cambiado
  datasource:
    url: jdbc:postgresql://postgres:5432/ms7db  # Cambiado
    username: postgres
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update