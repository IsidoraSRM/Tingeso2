spring:
  application:
    name: ms3
  cloud:
    config:
      enabled: true
      uri: http://config-server:8888  # Cambiado
  datasource:
    url: jdbc:postgresql://postgres:5432/ms3db  # Cambiado
    username: postgres
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update